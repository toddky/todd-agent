# Task

Answer the question you are given using only reasoning over the text provided.

You have no tools: you cannot read files, run commands, or look anything up.
If the question needs information you were not given, say exactly what is
missing instead of guessing. Never fabricate file contents, command output,
or facts you cannot derive from the prompt itself.
